package cli

import (
	"strconv"

	"github.com/cosmos/cosmos-sdk/client"
	"github.com/cosmos/cosmos-sdk/client/flags"
	"github.com/spf13/cobra"
	"newsletter/x/newsletter/types"
)

var _ = strconv.Itoa(0)

func CmdNewslettersBasic() *cobra.Command {
	cmd := &cobra.Command{
		Use:   "newsletters-basic",
		Short: "Query newsletters-basic",
		Args:  cobra.ExactArgs(0),
		RunE: func(cmd *cobra.Command, args []string) (err error) {

			clientCtx, err := client.GetClientQueryContext(cmd)
			if err != nil {
				return err
			}

			queryClient := types.NewQueryClient(clientCtx)

			params := &types.QueryNewslettersBasicRequest{}

			res, err := queryClient.NewslettersBasic(cmd.Context(), params)
			if err != nil {
				return err
			}

			return clientCtx.PrintProto(res)
		},
	}

	flags.AddQueryFlagsToCmd(cmd)

	return cmd
}
